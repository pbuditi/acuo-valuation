environments {
    dev {
        appId = 'webapp'
        env = 'dev'
        dataDir = 'src/main/resources'    
    }
    
    docker {
        appId = 'webapp'
        env = 'docker'
        dataDir = '/data'       
    }
}