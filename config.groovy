environments {
    dev {
        appId = 'valuation-app'
        env = 'test'
        dataDir = 'src/main/resources'    
    }
    
    docker {
        appId = 'valuation-app'
        env = 'docker'
        dataDir = '/data'       
    }
}